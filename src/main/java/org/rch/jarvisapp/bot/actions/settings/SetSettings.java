package org.rch.jarvisapp.bot.actions.settings;

import org.rch.jarvisapp.bot.actions.Action;
import org.rch.jarvisapp.bot.ui.Tile;

public class SetSettings implements Action {
    public String qwe;
    @Override
    public void run(Tile tile) {
/*        String[] data = actionData.getBody().split("_");
        tmpSettings.put(SettingsList.valueOf(data[0]).name(), data[1]);
        tile.stepBack();*/
    }
}
